name: CI

on: [push]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install Requirements
      run: |
        python3 -m pip install --upgrade pip
        brew install pipenv
        pipenv install
    - name: Update Migrations
      run: |
        pipenv run python3 /library/manage.py makemigrations
        pipenv run python3 /library/manage.py migrate
        
